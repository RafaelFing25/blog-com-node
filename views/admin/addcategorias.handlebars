{{#each erros}}
    <div class="alert alert-danger">{{text}}</div>
{{else}}

{{/each}}
<h3 class="card-title">Nova Categoria:</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/categorias/nova" method="POST">
            <label for="nome">Nome:</label>
                <input autocomplete="off" class="form-control"type="text" id="nome" name="nome" placeholder="Nome da categoria">
            <label for="slug">Slug: </label>
              <input autocomplete="off" class="form-control "type="text" id="slug" name="slug" placeholder="Slug Da categoria">
            <button type="submit" class="btn btn-primary  mt-4">Criar categoria</button>
        </form>
    </div>

</div>